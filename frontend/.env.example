# Backend API URL (required for production deployments like Vercel)
# Must match your uvicorn port (example: --port 8002)
VITE_API_URL=http://127.0.0.1:8002

# Google OAuth Client ID (Web application) from Google Cloud Console.
VITE_GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
